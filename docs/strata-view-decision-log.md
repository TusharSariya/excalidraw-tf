# Strata view — decision log

| Field | Value |
| --- | --- |
| Status | **Living document.** The running decision register for the Strata view (the rcll-v2 engine build). Appended at every work package and every milestone checkpoint — including owner visual verdicts and the binding arm-E verdict. Modeled on the v1 RFC's DEC-register + §34 as-built log. |
| Normative base | [`rcll-v2-spec-v2.md`](./rcll-v2-spec-v2.md) → [`rcll-v2-spec-v3.md`](./rcll-v2-spec-v3.md) → [`rcll-v2-spec-v3.1.md`](./rcll-v2-spec-v3.1.md) (later wins). This log records **decisions and their rationale**; the specs record the resulting normative text. On conflict the specs win. |
| Companion | [`strata-view-implementation-flow.md`](./strata-view-implementation-flow.md) (how each piece is built) |

Entry format: **SDEC-n · date · decision — rationale; rejected alternatives; provenance.** Inherited entries (Part I) carry the decision trail from the pivot memo through round 7 so this log is self-sufficient; live entries (Part II) start at this session.

---

## Part I — Inherited decisions (pivot → rounds 1–7)

**Engine shape & substrate**

- **SDEC-1 · 2026-06-23→07-04 · Build a NEW engine (rcll-v2 → Strata); do not fix RCLL in place.** RCLL's host is per-container recursion (Sander's documented anti-pattern) with a readability ceiling from the cyclic-provider dense-rank collapse; the debt is the toggle/config surface. Rejected: stay-and-prune (keeps the exoskeleton). *(pivot memo; assessment §2.2/§2.5; architecture-decision Options A–B; v2.0 D2′)*
- **SDEC-2 · 2026-07-04 · The v2 substrate (depth-pinned X = `columnX[rank]` + skyline Y) is the base.** Empirical: the Q2 7-arm A/B found **no rcll arm beats plain v2** on deviation/crossings/area — confirmed on a second preset in round 6 (the substrate decision is two-preset supported). Rejected empirically: "harden rcll instead". *(round-5 §4.2; v2.0 §0.7/§4; round-6 F3)*
- **SDEC-3 · rounds 1–3 · Global leaf ranking; hulls derive [min,max] column spans bottom-up and never take an independent X rank.** Supersedes the pivot's "SCC quotienting / co-axial bidirectional hull pairs" — hull co-axiality is emergent from derived spans; leaf-level cycles are A3's job. Rejected: independent per-hull coordinate frames (round-3 backward-edge failure). *(pivot Core Rules 1–2; architecture-decision CORR-1/-2/-3; v3.1 §7 supersession line)*
- **SDEC-4 · rounds 2–5 · Cyclic policy = corrected GreedyFAS (ELS93) + E′ contract + styled back-edges; A3 runs before ranking.** No clamp fallback exists in the new engine (v1's inherited clamp = the +77% width blowout, measured round 6; shipped-v2 keeps it — won't-fix, superseded). Provenance correction: greedy FAS is ELK's default, NOT graphviz/dagre's. *(v2.0 A3/D5′; round-6 F10/F11; v3.1 §7)*
- **SDEC-5 · rounds 3–4 + round 6 · Hull-Y coordination is a NO-GO (twice measured).** Moving whole hulls' Y to shorten cross-container edges fails because the feasible region is intrinsically 2D-inseparable (frames are subtree bboxes). Round 6: the NO-GO transfers to A7's LCA-level block nudge — A7 is a slice-A refiner only. *(rcll-y-axis-hull-coord-nogo campaigns; round-6 F1)*

**Objectives & metrics**

- **SDEC-6 · 2026-07-04 (round 5, owner) · READABILITY is the #1 objective; diff-stability is a hard constraint, not the objective.** Reversal trail: v1.0's D1 made proof-of-stability the M1 (round-4 posture, resting on the unshipped "editable seed" premise + over-read citations) → round 5 inverted it with the owner's explicit priority. Both round-4's "defer all readability" and naive "minimize crossings first" are rejected. *(v1.0 D1; v2.0 §0.1/D8′/D9; round-5 F11)*
- **SDEC-7 · rounds 2–6 · Crossing count is a diagnostic, never a gate.** Owner's revealed preference (prefers the highest-crossings arm for its deviation/height/containment wins) + corrected Kobourov/Ware readings. Round 7 adds: crossings-per-eligible-pair joins the arm-E owner-adjudicated table (the honest home for the one fooling-robust statistic — still never an automatic gate). *(v2.0 D9/§10; v3.1 §4)*
- **SDEC-8 · round 6 · The near-straightness objective is scope-split.** Cross-band edges are composition-floored (≈208px under frozen constants); near-straight gates slice A only; the cross-band objective (owner complaint) is owned by A0 (heights) + A2 (band order), measured by extent tails + bands-skipped; OD-9 routing is plan-of-record for the flow ceiling. The asymmetric prior (long-horizontal fine / long-vertical bad) is an **owner-calibrated engineering prior** with indirect literature support — recorded as such, not as established fact. *(round-6 F1/F2; v3.0 §2/§4)*
- **SDEC-9 · 2026-07-04 (round 7, owner) · Q0-TASK closed: the primary reader task is IMPACT TRACING** ("if X changes, what breaks downstream" — L→R dependency following). Calibrates gate weights (path continuity + cross-band extent primary), the Q7-AXIS hand-label design, and the arm-E verdict rubric. Secondary tasks (comprehension, PR review, debugging) remain report-informing. *(open since round 3; v3.1 front matter)*

**Determinism & identity**

- **SDEC-10 · foundation → v2.0 · Determinism ≠ diff-stability; diff-stability via stateless content keys, never previous-layout anchoring.** Rejected: DynaDAG/anchoring (path-dependent — C-IGDP is the literature name for what was rejected), libcola/IPSep as primary + VPSC-solver-primary (non-deterministic; VPSC feasibility *theory* kept), elkjs as the engine (not bit-reproducible), ML/LLM layout (non-deterministic, compound-blind — re-verified against 2025–26 literature in round 6). *(foundation §2; v2.0 §10; round-6 §7)*
- **SDEC-11 · round 5 · A6 identity constants:** generation-scheme versions (content-hash versions REJECTED — non-monotonic, stale peer wins reconcile), FNV-1a nonzero-clamped seeds (RoughJS seed-0 = unseeded), direction-preserving length-prefixed edge ids (endpoint-sorting collided A→B/B→A), injective SVG-safe frame-id encoding. Round 7 restores the lost `groupId = "tfg:"+key` line + groupIds in the id-rewrite list. *(v2.0 A6; v3.1 §6)*
- **SDEC-12 · round 6 · Tombstones live at the apply layer with `getUpdatedTimestamp()`** — the sole permitted wall-clock read (spec-literal tombstones are GC'd by `isSyncableElement`'s 24h window at the first broadcast; no bypass exists). Verified importable without layer violation (round 7). *(round-6 F4; v3.0 §6; round-7 lane C)*

**Algorithms**

- **SDEC-13 · round 5 · v1.0 algorithm rejections:** A2 Strategy-2/pure model order (the paper's own rejected configuration — "produces ONO layouts"), A3's double-reversed sink sequence (mechanically wrong), A4 absolute-|Δpos| gating (inverts Sondag). v1.0's deferred bidirectional sweep was later ADOPTED as A2's up-sweeps. *(v2.0 §0.2–0.6)*
- **SDEC-14 · rounds 5–6 · A2 is Strategy-1-INSPIRED, hull-scoped (Forster 02 / Sander 96); the same-layer barycenter term is load-bearing** (the only edge-aware input to band order). Forster 2004 (in-corpus, full text) is the designated upgrade (OD-12, post-M1). No border/dummy nodes in M1 (OD-3 Option A; Sander's ranking/spans inherited, not his placement). *(v2.0 A2; round-6 F1/F6; v3.0 §3)*
- **SDEC-15 · round 7 · A2's banded acceptance objective = height-weighted bands-skipped** (Σ per lifted edge of Σ(h_band + LANE_GAP_Y) over skipped bands — the exact t2 term; integer; computed from the candidate sequence alone). **Rejected with counterexamples:** v3.0's Σ extent over box-center chords (proxy provably diverges from the leaf-p90 gate; unbounded error in the tall-band regime), pure bands-skipped count (anti-optimizes extent 10× under heterogeneous heights), leaf-level p90 as objective (ties on ~20-edge order statistic → freezes at model order). Candidate set gains a height-aware greedy seed (generator/selector alignment); box-center chords survive scoped to the crossings tiebreak; root.policy pinned `banded` in the M1 schema copy (multi-provider seam). *(round-7 lane A + fable refute panel; v3.1 §1)*
- **SDEC-16 · round 7 · Gate statistics are operationalized, not aspirational:** slice-B extent gate = paired-per-edge bootstrap CI excludes 0 (address-keyed pairing, pinned seed); p90 gated only at n ≥ N_B,min; M4 low-coverage ⇒ INCONCLUSIVE requiring owner ack; M5 = ARI-stability with sklearn conventions (**not** Meidiana-CCQ — CCQ rewards change); ÷0/NaN/double-count defects pinned. *(round-7 lane B + panel; v3.1 §2–§3)*

**Recovered levers & scope dispositions (round 7)**

- **SDEC-17 · OD-14 height lever:** v1's rankSeparate (−42% height, cannot compose with NS — DI-NS-4) has no v2 equivalent and is NOT assumed subsumed by A0; designated post-M1 A/B if height/aspect miss. The round-6 matrix cell that mis-mapped it to OD-1 is corrected. **OD-15 de-band port:** v1's deBandLevel (membership-as-annotation) — pivot-named height keeper, silently dropped — port decision M3-adjacent. *(lineage audit; v3.1 §7)*
- **SDEC-18 · Scope dispositions:** T10 additionally reports shared-prep wall-clock (the all-resources RCA: two O(N²) prep spans dominate real cost — Q1's answer); Q5-TCO engine-endgame decision (does Strata retire rcll/compound/classic) owned at M2 planning; Q6-FIDELITY stays out of scope with the acknowledgment round 3 rated it potentially backlog-reordering; incremental layout REJECTED with rationale (stateless regenerate IS the design); saved-scene migration → S7/M3; variant-switch UX + LOD/minimap interaction → S0a acceptance notes. *(v3.1 §7)*

**v1 RFC DEC register (status at handoff to Strata)**

| DEC | Decision | Status → Strata disposition |
| --- | --- | --- |
| 1 | Forced-band Y-overlap on staircase | Implemented (hull level, ON) → subsumed by A0 banded policy |
| 2 | Cross-hull fan-out clamping (LCA centering) | Implemented in v1 → no v2 carry; hub centering = arm-E-comparative row, owner decision at M2 (v3.0 §8.3) |
| 3 | Default region policy | Deferred → A0 schema policy (packed default) |
| 4 | Aspect target & authority | Recommended → R5 + area-utilization report |
| 5 | Dummy nodes | OFF → OD-3A default; OD-3B slice-A escalation only |
| 6 | Crossing-metric fidelity | Implemented (polyline kernel) → T9 inherits `diagnosePipelineScene` |
| 7 | Huge fan-out | **Never implemented** → OD-13 layer-unzip A/B post-M1 |
| 8 | Cycle handling in SCC | Implemented (swimlanes) → replaced by A3 (structural) |
| 9/10 | Mirror-width / independence gap | Parked → not carried (no disposition needed; packed skyline changes the frame) |
| 11 | De-band ladder | Implemented (default none) → **OD-15 port decision** |
| 12 | rankSeparate height lever | Built, default OFF → **OD-14** (not subsumed) |
| 13 | rankSeparate whole-model ranking | Built, default OFF; cannot compose with NS → OD-14 (same lever family) |

---

## Part II — Live log (Strata build)

- **SDEC-19 · 2026-07-04 · View name = "Strata"** (button label, `view=strata` URL param, `pipelineLayoutVariant: "strata"`, own `layoutMode: "strata"`). Rationale: geological strata = ordered layers deposited deterministically — names the layered ranks, the banded rows, packed deposition in model order, and diff-stable regeneration. Rejected: Terrace, HCL (source-code confusion risk), Contour (names only the hull half). Internal spec name remains rcll-v2. *(owner)*
- **SDEC-20 · 2026-07-04 · Build scope = full M1** (D10 → S0a → M1a → M1b), hard-stop owner visual validation at V0/V1/V2, commit only on owner OK, codex review before each checkpoint commit. M1 exit includes the BINDING arm-E side-by-side verdict (v3.0 §9). *(owner)*
- **SDEC-21 · 2026-07-04 · Round 7 ran before any code** (owner directive: "if there is ANY ambiguity in the design we should dig into that first"). Output: [`rcll-v2-spec-v3.1.md`](./rcll-v2-spec-v3.1.md) — 19 amendments, GO on M1b under v3.1. M1b-gate implementation was blocked on this landing; D10 + S0a were GO throughout. *(owner + round-7 provenance)*
- **SDEC-22 · 2026-07-04 · View wiring design:** `"strata"` added to `TerraformView` + `TerraformLayoutMode` + `PipelineLayoutVariant`; rides the pipeline scene-builder family (`pipelineLayout = mode ∈ {pipeline, rcll, strata}`) with its own dispatch branch → `buildTerraformStrataExcalidrawScene`; threading covers all C6′ seams (sceneContext literal, toggle-guard literal, sceneApply mapper + skipLayoutCache allowlist, proof API + demo-URL param tables, the layoutMode clobber guard) + seam #6 (scene build bypasses `applyCompoundHierarchicalLayout`). Known silent-downgrade traps to test: preset-normalize ternary (→"semantic"), deriveLayoutModeFromView (→"module"), proof API hardcoded `layoutMode:"rcll"`. *(explorer-verified 15-point checklist, plan file)*
- **SDEC-23 · 2026-07-04 · OD pre-resolutions for M1 implementation** (agents must not resolve ODs silently): OD-1 NS = A/B at gate time (ship the passing arm); OD-2 K=4; OD-3 Option A; OD-4 per-SCC ON; OD-5 Option 1; OD-6 monotone skyline; OD-7 generation versions. OD-8 decided during S3 with a parity test; OD-9 owner decision before M2 freeze; OD-12/13/14/15 post-M1 A/Bs. *(specs' stated defaults; flow spec)*
- **SDEC-24 · 2026-07-04 · Battery realism:** the T9/Q2 battery runs with ancillary ON for at least one comparison arm per preset (v3.0 §8.6) while Strata M1 arms are extraction-free (ancillary ports at M3) — the honest asymmetry is stated in the flow spec and the arm-E verdict reads against it. Q7-AXIS (20-edge hand-label, both presets, impact-tracing framing) is scheduled before M2 gates freeze. *(v3.0 §8.5/8.6; SDEC-9)*

- **SDEC-25 · 2026-07-04 · W0/D10 bug-fix work package landed** (5 measurement-contaminating bugs, spec v2.0 §12). (1) Prep-cache fingerprint: bare-separator join + 40-char-prefix hash → `JSON.stringify` tuples + full-content djb2 (`hashString`); collision reproduced in test. (2) Sibling-edge comparator compared *different fields* of a/b (`a.sourceFrameId` vs `b.targetFrameId` — not antisymmetric) → extracted `compareCompoundTopologyFrameEdges`, C4′ code-unit chain (sequence→source→target→parent). (3) `randomInteger` `|| 1` nonzero clamp (RoughJS seed-0 = unseeded; all callers checked, none special-case 0). (4) shorten→compact demotion: **already surfaced** (`pipelineNetworkSimplexRankSuppressed` meta — round-6 note confirmed); added the missing `nsSkipReason:"cyclic-skip"` coverage. (5) v2-full+ancillary collision **root-caused and fixed**: the full cluster builder can anchor its frame at nonzero local (x,y) (satellite rows), the skyline packer assumed `[0,0]→[w,h]` and under-reserved by the offset → `clusterFrameLocalRect()` in `terraformPipelineV2Pack.ts` (leafBlock/blockXExtent/layoutBoxes use the true rect; byte-identical fallback for origin-anchored frames); v2 collisions 1→0, v2-compact/classic/compound byte-identical. Battery: 74/74 across the 5 touched suites + toggle-guard/threading suites clean; typecheck clean. **Intended behavior change:** v2-full scenes with offset frames get slightly larger/shifted (correct) reserves. **New finding, owner decision pending: `pipelineColumnPacking` is a silent no-op on classic/v2/compound variants** (only rcll consumes it) while scene meta echoes it as applied — options: (a) honest meta (suppress/flag the echo on non-consuming variants — small), (b) wire NS/columnCompact into the other builders (invasive), (c) leave flagged. *(D10 work package; agent-verified + orchestrator-reviewed diff)* **Codex review (W0 gate): one P1, fixed in place** — the frame-footprint fix mishandled NEGATIVE local-y offsets (`dropY` assumes blocks occupy [0,height]; a frame growing above its build anchor could poke into the block above — the symmetric case of the measured frameX=−18) → `leafBlock` anchor normalization (`anchorShift = max(0, −localRect.y)`; identity for origin-anchored and positive-offset frames); suites re-green. Orchestrator sweep: `build.width/height` naive reads remain in `terraformPipelineLayoutPacked.ts` + `terraformPipelineRcllPlacement.ts` — same latent class, different engines, pre-existing, OUT of D10 scope (flagged as follow-ups); full-mode `build.width/height` already equal the frame's own dims (`terraformTopologyLayout.ts:4764-4767`), so the anchor offset was the only discrepancy.

- **SDEC-26 · 2026-07-04 · W0 checkpoint verdict (owner): APPROVED — commit and start W1.** Battery at sign-off: 74/74 + 35/35 post-codex-fix, typecheck clean. **`pipelineColumnPacking` decision: honest meta in W1** — suppress/flag the echo (e.g. `pipelineColumnPackingInert: true`) on variants that don't consume it, folded into the W1 wiring work package; wiring packing into classic/v2/compound builders REJECTED for now (invasive, out of M1 scope); leave-flagged rejected (measurements stay contaminated). *(owner, W0 checkpoint)*

- **SDEC-27 · 2026-07-04 · W1b landed + a 16th wiring touch point discovered and fixed.** W1b (proof API + preset seeding) shipped: `layoutMode` param (allowlist rcll/strata/pipeline, default rcll byte-identical), strata param catalog (bool aliases + new int table for `strataSweeps`), `staging-extended-localstack-strata` preset. **Discovery: the preset SQLite schema had three `CHECK (view IN ('semantic','module','pipeline'))` constraints** — the strata catalog entry crashed the ENTIRE dev server at seed (and `'rcll'` was never in the CHECKs, latent because unused). Fix (orchestrator, outside both agents' scopes): widened fresh DDL + `migrateAddStrataViewConstraint` table-rebuild for existing DBs. **Second latent hazard found while fixing: the pre-existing pipeline migration DROPs the presets table with `foreign_keys=ON`, which fires ON DELETE CASCADE and silently wipes stacks/tfd child rows** — both migrations now wrap the rebuild in a pragma OFF/ON guard. Migration smoke-tested against a real old-schema DB with user rows: all rows survive, strata preset seeds, FK restored (the seed compactor's `DELETE FROM terraform_import_compositions` is pre-existing by-design derived-data behavior, not a migration loss). Flow-doc corrections: touch point 13b added; proof-API param is `preset=`, not `presetId=`. *(W1b agent + orchestrator)*

- **SDEC-28 · 2026-07-04 · W1a landed + fixture DB regenerated → V0 ready.** W1a shipped all 15 packages/ touch points: strata in all three unions + VIEW_OPTIONS radio; URL params (`strataNsRank`/`strataSweeps`/`strataCoordRefine`) parse+serialize+share-URL round-trip; deriveLayoutModeFromView + normalize-ternary trap regressions; dispatch → new `terraformPipelineStrata.ts` passthrough (delegates to v2 builder, meta `pipelineLayoutVariant:"strata"` + `strataPassthrough:true` + flag echoes); sceneContext literal + toggle-guard + sceneApply forwarding + skipLayoutCache allowlist + both variant-clobber sites; session/share-url/replay threading; `pipelineColumnPackingInert` honest meta (SDEC-26); `rcllV2Degraded` badge surfaces (dialog + demo) wired ahead of the W2 failure contract; deliberate deviation: no Advanced-settings panel for strata (S0a plumbing-only — flags have state but no UI controls yet). Batteries: threading 36/36, parity (strata AND rcll arms added) 16, dialog 35/35, D2′ spot-checks clean, typecheck ×2. **Fixture discovery: `getTerraformImportPresetSourcesFromDb` reads the committed TEST-FIXTURE DB** (not the live dev DB) for BOTH the proof API and the browser /sources route — the strata preset listed but 404'd on sources until `yarn export:terraform-presets-test-db` regenerated the fixture (now 5/5 hydrated presets). Live verification: proof API strata preset → view "strata", 1333 elements, variant+passthrough echoed; sources 200; rcll baseline unchanged (735); post-regen fixture suites 56/56. Housekeeping flag: stale worktree `.claude/worktrees/agent-a9facf0e2df3241d5` (branch fix/terraform-import-measurement-bugs, older session) double-runs vitest globs — left untouched pending owner disposal. *(W1a agent + orchestrator)*

- **SDEC-29 · 2026-07-04 · V0 checkpoint verdict (owner): "Validated — proceed."** Strata radio + v2-identical passthrough + share-URL round-trip confirmed visually on both presets + the strata-default preset. Codex review of the W1 diff: one P2 (dev-plugin int parser accepted oversized values → Infinity), fixed with `Number.isSafeInteger` + live-verified 400. Owner scope Q&A recorded: full view (compact=0) = supported by the M1 engine (A0 is card-mode-agnostic; D10 #5 unblocked it); all-resources/ancillary = M3 port with `strataAncillaryDeferred` honest meta in the interim + ancillary-ON comparison arms throughout + hub-extraction A/B at M2; de-banding = OD-15 port decision M3-adjacent. W1 committed; W2 (M1a engine core) started. *(owner, V0 checkpoint)*

- **SDEC-30 · 2026-07-04 · WP-2a landed (A3 cycle repair + A1 rank + S4 extraction) — orchestrator-reviewed, ACCEPTED.** `terraformPipelineStrataCycleRepair.ts` (exact ELS93 GreedyFAS per v2 §6-A3: per-SCC condensation OD-4, `s = leftSeq ++ rightSeq` NO reverse, F per SCC, E′ disposition-tagged with true-direction edges preserved for C10′ draw) + `terraformPipelineStrataRank.ts` (fresh sequence-free Kahn longest-path floor over effective E′; NS refinement reuses `computeNetworkSimplexDepths` behind the flag, committed only through the S4 gate; C1′ hard asserts). **Two deliberate fresh-implementations over reuse, both C4′-forced:** shared `stronglyConnectedComponents` sorts on bare cluster ids and shared `computeDepths` embeds `sequence` — both orderings the engine forbids; Tarjan + Kahn re-implemented with every tie-break through `addressOf` + the pinned comparator (adjacency lists, DFS roots, SCC representative, argmax ties). OD-4 multiplicity: weighted degrees in GreedyFAS; reaches the unmodified NS kernel by pair-repetition (it counts array occurrences). S4: `isDepthFloorValid` extracted verbatim from `applyDepthFloorIfValid` (diff-verified pure refactor). T7 fixture (ii)'s pinned arc hand-derived in-test (2-cycle argmax tie → comparator-least `a` first ⇒ `b→a` reverses). C1′ "missing rank" dev-assert proven structurally unreachable via the public API; nearest reachable assert (endpoint outside universe) tested instead. Battery: 12/12 new tests, D2′ 66/66 (rcll/v2/pipeline suites), typecheck clean. *(WP-2a agent + orchestrator diff review)*

- **SDEC-31 · 2026-07-04 · WP-2d landed (T9 slice-A/B metric family + baseline derivation) — orchestrator-reviewed, ACCEPTED.** New `terraformPipelineSliceMetrics.ts` (`computeSliceMetrics(elements)` — pure scene-side measurement); diagnostics + Q2 harness touches diff-verified purely additive (0 removed lines); policy map **imported** from `STRATA_HULL_POLICY`, not re-declared (v3.1 §2.6). **Real bug caught by the agent's verification probe:** COMPACT-mode compound scenes stamp primary-cluster frames with `terraformCompoundParentKey` ONLY (no `terraformTopologyPath`) — the spec's premise was false; unfixed, every compact-arm edge classified slice-B (nA=0 verified pre-fix). Fixed with 3-tier `resolveClusterPath` (own path → parent-key lookup → geometric containment), all tiers regression-tested. **Six measurement conventions pinned (orchestrator-adjudicated, to be folded into the S0b freeze register):** (1) extent = polyline max_y−min_y (matches existing diagnostics convention, per spec's own instruction); (2) Δx_endpoints = |x_last−x_first| of rendered points; (3) content bbox = same element set as `dataflow.aspect` (topology ∪ primary-cluster frames); (4) band Y-position: real topology frame rect wins over dissolved-band member-median; (5) degenerate same-band slice-B edges report 0, never negative/undefined; (6) percentiles = nearest-rank `sorted[floor(n·f)]` (reproduces the existing p50 exactly). **Baseline table derived on the v2 substrate (arms A/F × presets P1/P2), populations exact (`nA+nB+unresolved == tfdArrowCount` in all 4 cells)** — headline: P1-A nB=37, P1-F nB=114, P2-F nB=32 clear the N_B,min≈30 target; **P2-A (compact staging-localstack) is underpowered at nB=4** — any C11 gate on that cell must exclude it or mark it underpowered. Battery: 17/17 new tests, Q2 4/4 (full 7-arm run), readability 16/16, typecheck clean. One orchestrator nit-fix post-review: the `perHull` report sort used bare `localeCompare` (locale-dependent ordering in a frozen-baseline artifact) → code-unit compare; suite re-green 17/17. *(WP-2d agent + orchestrator diff review)*

- **SDEC-32 · 2026-07-04 · A2 lift semantics ruling (orchestrator, normative interpretation): lifted E′ edges are a PER-EDGE MULTISET — no (from,to) dedup; multiplicity does NOT weight the cost.** WP-2b's first delivery deduped `liftStrataEdgesToUnits` by directed unit pair; v2.0 §6-A2's lift text ("edge u→w contributes iff its endpoints lie in different units of h") and v3.1 §1.1 ("Σ over lifted E′ edges") both give per-edge semantics — under dedup a 20-edge bundle between two bands weighs the same as a single edge, materially changing the M1b acceptance objective (and classic barycenter adjacency is also per-edge, which the WP-3a sweeps inherit). Companion ruling: each StrataEdge in E′ (already parallel-deduped by the model) contributes exactly ONE lifted term — `multiplicity` stays scoped to ranking (A3 step 0's "ranking weight"), never the A2 cost. Inert at K=0 (M1a selects nothing), fixed now because it is the M1b machinery. Fix dispatched back to WP-2b (owned files) with a determinism pin: lifted entries carry the underlying edge key, sorted (from, to, key) via the pinned comparator (v3.1 §1.5). **Fix landed and orchestrator-verified same day:** dedup removed, both rulings pinned in the lift's doc comment, tests 9→11 (multiset order, exact 2× cost doubling, multiplicity-yields-one-entry); incidental hygiene fix — WP-2b's first delivery had embedded literal NUL bytes (broke grep/git text handling), `STRATA_ROOT_ID` now printable `"__strata_root__"` (family-consistent with `RCLL_ROOT_KEY`); orchestrator re-ran: NUL scan clean across all Strata files, 26/26 Strata tests, typecheck + eslint clean. WP-2b fully ACCEPTED. *(orchestrator spec adjudication + WP-2b fix)*

- **SDEC-33 · 2026-07-04 · S0b freeze register written (v3.1 §12) — the C11 precondition for all gated code is now satisfied.** Frozen by amendment before any gate lands: slice-A/B baselines (4 headline cells + full 7-arm frozen JSONs committed at `docs/strata-baselines/` with SHA-256 pins); **N_B,min = 30** (distribution-free order-statistic rationale — no per-edge extent dump exists to bootstrap directly; W3 CI harness must report realized CI width as a stability self-check, degenerate CI voids the cell); **new pin: nB < 10 excludes a cell from ALL slice-B gating** (P2-A at nB=4 is out — P2's slice-B gates run on arm F only); **N_min = 20**; **M4 coverage floor = 0.5**; **bootstrap seed 20260704 + mulberry32 + B=1000 + percentile CI + 20% unmatched-void threshold**; A2 candidate order restated; WP-2d's six measurement conventions adjudicated in. Items 3/5/void-threshold are new numeric pins under delegated freeze authority — **flagged for owner review at V1**. *(orchestrator, spec amendment)*

- **SDEC-34 · 2026-07-04 · WP-2c landed (P8 scene build + P11 failure contract + M1a engine integration) — orchestrator-reviewed, ACCEPTED, with one integration defect found and root-caused by the orchestrator.** New `terraformPipelineStrataSceneBuild.ts` (leaf skeleton-origin pre-compensation via `clusterFrameLocalRect`; hull frames emitted directly at A0 boxes with content-addressed ids + `terraformTopologyPath` stamps; SEAM #6 honored — `applyCompoundHierarchicalLayout` never called, provider-Y byte-identity test on a ≥2-provider fixture); `terraformPipelineStrata.ts` passthrough → real engine (stages model→a3→a1→a0/a2→scene-build→structural-check; any throw → v2 fallback WITH the already-built `prep` + `rcllV2Degraded {stage, reason}`; `strataAncillaryDeferred` honesty; `strataPassthrough` removed); v2 builder gains `options?.prep` (v3.1 §5) with a D2′ scene-equality test. Accepted deviations: edges drawn from `prep.collapsedEdges` in true direction (C10′ — `edgesPrime` is a rank/order concept; back-edge styling is M1b/A6); prep itself NOT inside the failure contract (a prep throw propagates loudly — the §5 "fallback without prep" would rebuild the same prep and re-throw, so degrading is dishonest there). **Integration defect (NaN geometry): wiring the engine into `terraformPipelineStrata.ts` pulled the Strata modules into the PRE-EXISTING `terraformPlanParsing → terraformLayoutCore` import cycle** — with `terraformPipelineLayoutShared` still mid-evaluation on that path, Placement's module-level `STRATA_TITLE_RESERVE = PIPELINE_FRAME_PAD * 2` froze as NaN (all non-root hull Y/height NaN; import-graph-entry-dependent, which is why WP-2b's suite was green pre-wiring and red post-wiring — WP-2c misattributed it to WP-2b; instrumented dump + madge pinned the true cause). **Fix (orchestrator): Placement's three derived spacing consts became call-time reads** (matching the RCLL precedent: never derive module-level consts from imported consts inside the cycle). `terraformPipelineV2Pack.ts` carries the SAME latent pattern (HULL_GAP_Y/HULL_TITLE_BAND/V2_COLUMN_GAP) — pre-existing, benign under today's real entry order, flagged as a follow-up, NOT touched under D2′. *(WP-2c agent + orchestrator root-cause + fix)*

- **SDEC-35 · 2026-07-04 · W2/M1a battery PASSED — first real Strata geometry on both presets, both card modes, V1 ready.** Orchestrator added the Q2 strata arms (G_strata_k0 compact + G2_strata_k0_full + cyclic G arm; strata meta echoes hand-added per v3.1 §2.6). Results: **no `rcllV2Degraded` anywhere** (both presets × both card modes + the injected-2-cycle fixture — A3 repaired it end-to-end with geometry byte-identical to the acyclic run); **`strataStructural` all-zero everywhere** (R2); collisions 0; element counts equal v2's exactly (1333/735 compact, 4845/3728 full — R3); slice populations identical to v2 (108/37, 65/4 — topology-derived LCA sanity); T10: engine ≈100–220ms warm, prep dominates cold (matches the RCA). **Honest K=0 readability (expected, not gated at M1a):** slice-B extent p50 9665 vs v2's 3166 (P1 compact), crossings 273 vs 177, taller canvas (19066 vs 10056 px) — pure model-order bands with zero ordering optimization; W3's K=4 + greedy seed + A7 exist to earn this back, and the frozen §12 baselines are the bar. Suites: 48/48 Strata, 88/88 D2′+threading+parity+churn, Q2 4/4, slice-metrics 17/17, typecheck ×2, lint clean. *(orchestrator, W2 battery)*

- **SDEC-36 · 2026-07-05 · Codex review of the W2 diff: no P1s; two P2s, both fixed in place.** (1) A2 stage attribution: a failure originating in the ordering pass (or the forced-error hook at "a2") was tagged `a0` in `rcllV2Degraded` — fixed with an honest per-stage gate order + an attribution contract: **any throw WP-3a adds to the ordering module MUST carry the "Strata A2" message prefix** (the entry point re-tags by prefix, since A0+A2 execute inside one `placeStrataHulls` call). (2) Successful cyclic layouts emitted the LEGACY `pipelineCycleWarnings` text ("order fell back to first .tfd occurrence") — false for Strata, which repairs structurally; replaced with an accurate Strata warning emitted only when `repair.feedbackKeys.size > 0` ("N cycle edge(s) repaired structurally (GreedyFAS); drawn against flow direction"). Post-fix: scene-build 10/10 + threading 4/4, lint + package typecheck clean. **W2 code-complete — STOPPED at checkpoint V1 for owner visual validation (commit only on OK).** *(codex + orchestrator)*

- **SDEC-37 · 2026-07-05 · V1 checkpoint verdict (owner): "looks good to me, exactly what i would expect from an unoptimized layout."** First Strata-native geometry (K=0 model-order bands) visually validated on both presets. The three delegated freeze pins flagged at V1 (nB<10 slice-B gating exclusion, M4 coverage floor 0.5, 20% unmatched-void) drew no owner objection — they stand as frozen. W2 committed; W3 (M1b: A2 K=4 + A7 + A6/tombstones + A4 gates + arm-E table) started. *(owner, V1 checkpoint)*

- **SDEC-38 · 2026-07-05 · WP-3c landed (A6 deterministic finalize + apply-layer tombstones) — orchestrator-reviewed, ACCEPTED; OD-8 adjudicated INTEGRATE.** New `terraformPipelineStrataFinalize.ts` (+24 tests): the spec-verbatim id scheme (`tf:`+role incl. `group` per v3.1 §6; length-prefixed direction-preserving edgeId; injective percent-encode frame-id sanitization with a stripping-collision regression test), FNV-1a seeds/`versionNonce` with nonzero clamps, generation versions (OD-7), and the FULL id-reference rewrite (boundElements/containerId/frameId/start+endBinding/**groupIds**) with uniqueness + dangling-ref dev-asserts BEFORE restore can silently randomize dups. Wired as a pure post-pass in `buildStrataScene` (single call site — the spec's "both call sites" is satisfied vacuously: strata converts only via `convertPipelineSkeletonToElements`, seam #6 bypass means the compound converter never runs, and the v2 fallback deliberately keeps old identity under D2′). Tombstones in `terraformSceneApply` immediately before `replaceAllElements`: removed = prevAddresses − newAddresses, `isDeleted:true`, `version = G_B`, strata-scoped by gating on canonical ids in the INCOMING scene (degraded-v2 scenes never tombstone); one-generation window via a `terraformStrataTombstone` customData marker (accepted implementation detail — soft-hidden `isDeleted` elements still count as present addresses, dedicated test). **OD-8 adjudication: INTEGRATE** — `repairTerraformEdgeBindings` never touches ids (it re-derives geometry/bindings from relationship ADDRESSES with an `arrowGeometryEqual` short-circuit); finalize rewrites ids and never touches geometry; the parity test OD-8 demanded exists as an empirical assert that repair-after-finalize is a byte no-op on the real preset. Caveat recorded: apply/reconcile still bump versions randomly on RUNTIME visibility changes (pre-existing, all engines) — A6 governs build-output identity only. **Accepted interpretations:** (a) G default = 1, no caller threading (plan JSON has no state serial; the app-side counter is the S7/M3 follow-up; machinery fully G-parameterized and tested at G>1); (b) `updated` normalized to constant 1 scene-wide — C3′-compliant ("no updated timestamps") and REQUIRED because the shared icon-injection kernel stamps wall-clock values; this is what makes T1 byte-equality hold with no tolerance; (c) icon-glyph/inner-group ordinal = in-block first-appearance rank, accepted as content-derived (the block is cloned verbatim from the committed icon-library asset, so in-block order IS asset content order, not scene position); (d) frame addresses = role + per-segment-percent-encoded topology path (injective join). Known nit (logged, not blocking): `__testForceStageError:"finalize"` has no matching `gate()` call and silently no-ops — finalize-stage attribution is exercised via the "Strata A6" message-prefix contract instead (mirror of "Strata A2"). Agent-verified: 99/99 across strata+apply suites incl. T1 full byte-equality on the real preset, D2′ 5/5, typecheck clean; orchestrator battery re-run deferred until WP-3a/3d land (shared tree is mid-edit). *(WP-3c agent + orchestrator diff review)*

- **SDEC-39 · 2026-07-05 · WP-3d landed (A4 churn metrics + A5 pierce/contiguity + pinned bootstrap-CI harness) — orchestrator-reviewed, ACCEPTED. All measurement-only; no layout behavior touched.** Three new modules (+48 tests). **`terraformPipelineBootstrapCi.ts`**: mulberry32(20260704) with an in-file KAT (orchestrator re-derived the first three outputs independently — match), B=1000, percentile CI [2.5,97.5], pairing key = canonical terraform edge address (true direction + relKind, NUL-joined), unmatched excluded+counted, void at >20%·min(arm) — every §12 pin implemented exactly, constants exported as the single source of truth; `bootstrapGatePolicy` encodes nB<10 ⇒ report-only / 10≤nB<30 ⇒ gate-p50-report-p90 / nB≥30 ⇒ gate p90 UNLESS degenerate (upper==sample-max) ⇒ fall back to p50 — the orchestrator re-checked §12's wording ("voids the p90 gate for that cell", not the cell) and the p50 fallback is the correct reading. **`terraformPipelineStrataChurnMetrics.ts`**: M1_rel exactly Sondag (ordered U×U pairs, ½·Σ|Δ 8-sector area fractions|, degenerate boxes as center points, hand-derived worked example 0.40625); M2_flip as a rate over pairs sharing a column in BOTH layouts; M3 report-only; M4 with all three shift tiers (own-column ≥3 → ±1 rank-band → global, componentwise medians), coverage ratio, and explicit ok/inconclusive/vacuous statuses (never a silent downgrade); M5 = 1−ARI with sklearn degenerate-⇒-1 conventions, unclamped before gating, and — per v3.1 §3.2's normative "computed independently within layout x" — parent-hull partitions by GEOMETRIC deepest-containment per layout (the agent independently derived why the semantic-path alternative is degenerate: U's hash already pins the parent path, so M5 would be ≡0 by construction). **`terraformPipelineStrataPierceMetrics.ts`**: A5 pierce via Liang-Barsky clip + clipped-midpoint strict-interiority — one rule that provably yields all three normative boundary semantics (each unit-tested separately); ancestor exclusion = path-prefix against both endpoints; report-only during M1 per §6-A5. **Adjudicated interpretations (logged):** (a) content-hash stand-ins — incident E′ endpoints → incident rendered-TFD-arrow direction-tagged tokens (engine-invariant: the v2-substrate baseline arms have no E′ at all; rendered arrows draw true direction, mirroring §2.5's pairing-key philosophy), parent path → the WP-2d 3-tier resolution, collapse class → `terraformCollapseClass` else role stand-in until S10 stamps it; renames remap the node's own address AND incident endpoint tokens before the U intersection (required for "the renamed node stays in U" to extend to its neighbors); (b) M2 pairs with a Y-tie in either layout are excluded from numerator AND denominator (undefined order); (c) contiguity violations counted as runs−1 per (hull,column) — a finer count than the spec's boolean, 0 exactly when contiguous; (d) column identity = exact bbox-left-edge equality (the slice-metrics convention). Scene readers duplicated from sliceMetrics per that file's own additive-only precedent; C4′ comparators throughout. **Orchestrator hygiene fix on a WP-3c file found during the review's NUL scan:** `terraformPipelineStrataFinalize.ts` carried two RAW NUL bytes in string literals (the WP-2b Write-corruption class; WP-3d had proactively escaped its own) → rewritten as `"\0"` escapes, byte-identical runtime semantics. Verification: 72/72 (bootstrap 15 + churn 21 + pierce 12 + finalize 24 re-run post-fix, incl. the real-preset T1/OD-8 integration tests), agent-side full typecheck + eslint clean. *(WP-3d agent + orchestrator diff review)*

- **SDEC-40 · 2026-07-05 · WP-3a landed (A2 K=4 sweeps + height-aware greedy seed + policy-keyed acceptance) — orchestrator-reviewed, ACCEPTED, incl. an orchestrator-caught integration gap fixed under a scope amendment.** `orderStrataUnits` extended (signature unchanged; two ADDITIVE-OPTIONAL params: `unitXSpanOf` for trial-chord x-centres, `unitColSpanOf` for sweep layers — the latter a necessary addition beyond the brief, accepted). **Spec-exact against v3.1 §1:** banded acceptance = weighted bands-skipped cost (§1.1 exact t2 term, prefix sums, integer px, chord-free), crossings-trial tiebreak on cost ties ONLY (§1.2 — banded-stack simulation, box-centre chords in a DOUBLED-integer coordinate system so orientation cross-products are exact; shared-endpoint pairs excluded per the eligible-pairs convention; proper crossings only), remaining ties → earliest candidate; candidate set {initial, chained sweeps 1..K, height-aware greedy seed} in pinned order (§1.3; greedy seed inserts in pinned key order at the cost-minimizing position, ties → earliest position); acceptance keys on POLICY alone, no root special case (§1.4); lifted edges iterated in pinned (from,to,key) order (§1.5). Packed hulls keep the v2.0 per-sweep strict-crossings-decrease chain and ignore the bands-skipped scorer AND the greedy seed (both banded-objective devices — documented reading, accepted). §6-A2 sweep machinery: barycenters PER-EDGE over the multiset (a 20-edge bundle = 20 adjacencies, SDEC-32 honored), swept-direction (down=IN/up=OUT) ∪ same-layer with no double-count, no-relevant-edge units hold their own position (NaN-safe generalization of the no-neighbour float), single batch re-sort per sweep by (bary, key, unitId), K=4 alternating down/up. **K=0 byte-identity: returns the sorted input unit objects BY REFERENCE** (asserted `.toBe`) — the frozen Q2 strata@K0 arm and V1 geometry untouched. All 9 mandatory fixtures present (21 tests): ≥2-provider banded root, band-adjacency, heterogeneous-height (the round-7 counterexample class), x-flip proving the tiebreak drives selection, full-tie→initial, greedy-seed-wins, packed accept/reject, multiset rulings, run-twice determinism; expected sequences cross-derived on a standalone replica before hardcoding. **Orchestrator-caught gap → scope amendment: the two optional params were NOT wired at the real call site** (`placeStrataHulls`), so in-engine K>0 hit the degenerate fallbacks — MATERIAL because region/vpc/subnetZone are all packed policy: all-x=0 collinear chords ⇒ crossings always 0 ⇒ packed could NEVER accept a sweep (K=4 ≡ K=0 silently); banded tiebreak degraded to earliest-wins and layer-0-for-all merged sweep directions. Fix landed same day: placement passes real `[x0,x1]` + `colSpan` from its `UnitInfo` (?? -degenerate convention matching `unitHeightOf`, no new throw ⇒ "Strata A2"-prefix contract satisfied vacuously) + two placement-level proofs-of-life: (b) a packed crossing fixture where K=4 flips the column stack (trial 1→0) and the SAME direct call WITHOUT geometry provably cannot leave model order — the fixture also proved x-alone insufficient (both params exercised); (c) a REAL banded behavioral difference found by brute-force + hand-trace (4 providers, ranks {0,0,1,1}: real layers let up-sweep 2 reach the cost-0 order; the all-layer-0 degenerate oscillates at cost 756 and the greedy seed wins with a different order). **Interpretations adjudicated:** sweep-relevance union without double-count (an edge both swept-direction and same-layer contributes once); banded sweep chain has NO per-sweep gate (all K states are candidates — §1.3's set is the gate); packed acceptance chain advances only on a win. Battery: ordering 21/21, placement 8/8, scene-build 10/10, full strata family + apply + threading 111/111 (orchestrator re-run), typecheck + eslint clean. K=4 default flip remains an OWNER decision at V2. *(WP-3a agent + orchestrator diff review + scope amendment)*

- **SDEC-41 · 2026-07-05 · WP-3b landed (A7 per-hull Y refinement, OD-5 Option 1) — orchestrator-reviewed, ACCEPTED. All 7 orchestrator pins implemented as pinned; no pin overridden.** New `terraformPipelineStrataCoordRefine.ts` (+18 tests): `refineStrataCoordinates(placement, model, edgesPrime)` — a PURE placement→placement transform called from the engine between placement and scene build behind `strataCoordinateRefine` (integration option (i), accepted: flag-off byte-identity is STRUCTURAL — the function is simply never called — and the module auto-joins the C3′ static-scan family). Method verified against §6-A7's normative details: per-column Jacobi batch (targets from the pre-projection snapshot; a fixture proves Gauss-Seidel would differ), median targets over swept-from-SIDE neighbours at current centres (pin P2 — strictly-less/greater home column; same-column partners excluded from the median, counted in acceptance), PAV isotonic projection in gap-removed coordinates + monotone box clamp with fixed-x-overlapping-block clamps and empty-feasible ⇒ reject (P4), acceptance = strict decrease of the hull-global Σ|Δy| (computed over ALL the hull's chords — mathematically equivalent to the spec's "touching the swept column" since non-touching chords are unchanged; documented), d/u/d/u sweeps (P3), bottom-up child-rigid nesting with LCA chord lifting (elegant: `liftStrataEdgesToUnits` per hull with that hull's cluster→direct-block map lands each chord at EXACTLY its LCA — ancestors collapse both endpoints into one block, descendants lose an endpoint), re-anchor = per-hull height recompute (A0 step 5) + top-down absolute re-application rooted at PIPELINE_MARGIN (orchestrator-verified identical to A0's `assignAbsolute` anchor — a no-move refinement is a true identity), and the in-engine R2 dev-assert immediately after re-anchor throwing with the "Strata A7 " prefix (stage "a7" + catch re-tag wired in Strata.ts, mirroring the A2/A6 contracts). **Constraint-preservation argument (orchestrator-checked):** within-column A2 order holds by INDUCTION — each projection is monotone in the current order, so current order ≡ A0/A2 order throughout; same-home-column blocks always share x0 (columnX), so the min-gap chain never over-constrains x-disjoint pairs. **Two engineering choices within pin latitude, accepted:** (1) PAV-then-monotone-clamp (feasible + deterministic, not the exact box-constrained LSQ optimum — unnecessary since acceptance re-gates on the objective); (2) a downward-only order-preserving seed-compaction before sweeps — identity on feasible A0 layouts, restores feasibility when a refined child hull GROWS (the mechanism that makes bottom-up growth propagation R2-safe; growth is anticipated by the spec's own extent-recompute). Hull box grows to fit (constraint (b) read as band-with-growth, consistent with A0-step-5 re-run; any extent regression is the T9/gate's to catch). Even-count median PINNED = mean of the two central order statistics (any point in the central interval is L1-optimal — a determinism tiebreak). **One bug found + fixed during the agent's own verification:** the projection's gap chain initially used bare minGap instead of height+minGap (top-to-top contract) — banded region bands collapsed, R2 broke on both presets; fixed, R2 = 0 both presets flag-ON. Objective proof (test 9, hand-derived): Σ|Δy| 90 → 25 on the subnetZone fixture. Battery: coordRefine 18/18; **orchestrator consolidated W3 battery 177/177 across all 14 strata-family suites** (ordering/placement/scene-build/model/cycle-repair/rank/finalize/apply/threading/bootstrap/churn/pierce/coordRefine); full `yarn test:typecheck` clean; NUL scan clean. **Flag default stays OFF — the T2+R4 gate is evaluated in the orchestrator battery next; the default flip is an owner decision at V2.** *(WP-3b agent + orchestrator diff review)*

- **SDEC-42 · 2026-07-05 · W3 battery caught a real A6 defect (full-card-mode duplicate stable ids) — root-caused as SATELLITE APPEARANCES, fixed by WP-3c under the spec's own "satellite sub-address" clause, ACCEPTED.** The 13-arm Q2 run showed all three FULL-mode strata arms degrading at `stage:"finalize"` — `tf:node:aws_ecs_cluster.this` derived for two elements (the uniqueness dev-assert working as designed: honest degradation to the v2 fallback, no silent id randomization; but full-mode battery columns were vacuous — all three arms fell back to byte-identical v2 scenes, visible as identical metric rows). WP-3c's compact-only preset coverage was the gap. **Verified root cause (orchestrator hypothesis confirmed):** full mode renders a resource as a tier-1 SATELLITE card inside multiple cluster builds — 16 duplicated visibility keys on P1, all satellites; labels duplicated identically and icon ordinals silently continued one shared counter (the two companion roles the orchestrator flagged). NOT multi-account primary reuse (zero duplicate primary addresses; satellite-cluster frames already unique via role:path). **Fix (appearance-qualified addresses):** `appearance = key` for primaries and every compact card; `appearance = key + ":#" + parent` for satellite appearances — the v2.0 §6-A6 "ordinal = content-derived (satellite sub-address)" clause implemented literally; node/label/icon/pin-group/icon-group ids all appearance-scoped, icon in-block counters restart PER appearance. **Accepted carrier deviation from the orchestrator's pinned direction, evidence-backed:** frameId→primaryAddress is NOT total (satellite cards can carry `frameId:null`), so the carrier is the element's own `customData.terraformExplodeParent` (stamped on satellite rects, mirrored onto detached labels; `(visibilityKey, explodeParent)` empirically injective scene-wide; icons resolve through their card's pin group, claimed by exactly one rect). Compact byte-identity holds BY CONSTRUCTION (null parent ⇒ bare key) + explicit regression test. **Two shared-kernel accommodations, no upstream files touched:** (1) `injectTerraformAwsIconsIntoElements` resolves detached labels by BARE key, so one appearance's label gets stamped with the other's pin group → disposition: the RECT is the authoritative pin-group owner (dev-assert: no two rects claim one pin); labels keep self-derived identity and never key a pin group; (2) full-mode icon templates carry start/endBindings referencing the LIBRARY ASSET's ids (`cloneIconElements` nulls boundElements/containerId but not bindings) → finalize normalizes dangling bindings to null ON ICON GLYPHS ONLY; every non-icon dangling binding remains a hard dev-assert (both directions unit-tested). Same-direct-parent duplicates would still throw (honest degradation, documented). New tests: full-mode preset integration incl. the missing full-mode T1 run-twice byte-equal + scene-wide uniqueness; synthetic two-cluster satellite fixture (distinct ids, per-appearance ordinals, permutation-independence, re-finalize fixpoint); compact regression. Battery: 117/117 strata family, typecheck + eslint + NUL scan clean; orchestrator Q2 re-run with valid full-mode arms in flight. *(orchestrator battery finding + WP-3c fix)*

- **SDEC-43 · 2026-07-05 · WP-3e landed (A4 fixture-triple harness) + A4 thresholds FROZEN (v3.1 §13) + A7 gate PASSES.** New `terraformPipelineStrataChurnTriple.test.ts` (harness/report only, no layout behavior): the three §6-A4 mutations on P1 (derive) + P2 (validate) × arms {v2, K0, K4, K4+A7}, all health checks green, |U| ≥ 68 everywhere, no strata degradation. **Two load-bearing engine facts the harness surfaced (documented in its header):** (1) `preparePipelineLayout` builds clusters EXCLUSIVELY from tfd-declared-dataflow endpoints — a plan-only resource never becomes a cluster, so add-one-resource must wire the new resource into tfd (bind + fanout-widen) to bite; (2) moved{}-rename must rename BOTH `resource_changes` and `prior_state`'s module tree — `buildExistingEdges` synthesizes a phantom "existing" node for any prior_state address left behind. **Headline results:** K=0 is ZERO-churn on every cell (perfect diff-stability, the content-key design working); K=4/K=4+A7 beat the v2 anchor on M2_flip both presets (P1 0.0050 vs 0.0069; P2 0.0544 vs 0.1514 — v2's P2 add-one-edge order churn is severe, ×2.8 worse); rename is zero-churn for ALL strata arms (the address-keyed identity chain end-to-end); M5=0 + ARI=1 everywhere (no hull-membership churn); m4/m5 statuses all "ok". **A4 thresholds frozen by amendment (v3.1 §13): M1_rel ≤ 0.08, M2_flip ≤ 0.10** — derivation trail is honest about a FAILED P1→P2 transfer (P1-only rule gave M2_flip 0.02; P2's structurally harsher add-one-edge broke it, v2 shows the same ×22 jump) → joint freeze with 2–3× headroom over worst candidate cells, bounded by v2-class churn failing; thresholds gate M1_rel/M2_flip only (the §6-A4 PASS clause), flagged for owner review at V2. **A7 gate (§6-A7) PASSES both halves:** R4 — fractionNearStraight strictly up AND mean deviation strictly down on ALL three presets (Q2: 0.13→0.14/2056→2031, 0.03→0.04/1848→1821, 0.03→0.04/1311→1270; slice-A p50/mean improved everywhere; zero canvas growth); T2 — churn not regressed (I ≡ H on every cell except one far-under-threshold M1_rel hair). Recommendation to owner at V2: ship A7 ON. WP-3e extended with the final measurement piece: the §2.5 per-edge paired bootstrap-CI extent-gate harness (additive `perEdge` export on sliceMetrics + `terraformPipelineStrataExtentGate.test.ts` over the §12 gate cells). *(WP-3e agent + orchestrator threshold adjudication)*

- **SDEC-44 · 2026-07-05 · §2.5 slice-B extent gate RUN (WP-3e extension) — the gate does NOT pass; the spec's own Σ-vs-tail backstop fired. Full analysis routed to the owner at V2.** Machinery accepted: additive `perEdge` export on sliceMetrics (37 insertions, 0 deletions; 17/17 existing tests unmodified; SHA-pinned baseline JSONs untouched; aggregates provably recomputable from rows) + `terraformPipelineStrataExtentGate.test.ts` (paired per-edge bootstrap CI per §2.5/§12 pins; determinism double-computed byte-identical; §12 baseline populations reproduced exactly: 37/114/32/4). **Gate outcomes:** (1) **P1-compact A-vs-H/I (the one cleanly-pairable frozen cell, 37v37, 0 unmatched): FAIL** — mean paired delta +1008px [CI −79, +2282] (straddles 0, wrong direction); paired p50 IMPROVES −637 but paired p90 REGRESSES +6369 — a tail-regression signature. The Q2 aggregate p50 win (2529 vs 3166) is real but the tail dominates the paired comparison. Structural read: strata's P1-compact canvas is ~1.9× taller than v2 (19066 vs 10056 — v2 packs 50 side-by-side rows; strata's banded provider/account levels stack full-width bands), so a handful of cross-band edges span nearly the whole canvas; A2 minimizes weighted bands-skipped, not total stack height — exactly the Σ-vs-tail residual v3.1 §1.1 said the slice-B gates backstop. The post-M1 levers for this are ALREADY REGISTERED: OD-14 (height lever, DEC-12-class rankSeparate −42% height A/B) and OD-15 (subnet de-band port). (2) **All four full-mode F-cells: primary pairing VOIDED by the frozen 20%-unmatched rule — slice classification is NOT engine-invariant in full mode** (F-with-ancillary classifies 114/32 edges slice-B via hub-extraction hull changes; strata keeps the compact split 37/4) — the SDEC-24 honest asymmetry reaching the gate machinery; the void rule worked as designed. WP-3e's clearly-labeled SUPPLEMENTARY pairing (baseline keyset, candidate extents regardless of classification; adjudicated REPORT-ONLY companion, primary stays frozen) shows the full-mode direction: **P1-full nearly passes** — I2 CI [−1739, +15.5] (15.5px from excluding zero), paired p90 −6668 IMPROVING, and A7 visibly moves the needle vs H2 (+108.7 → +15.5); P2-full worsens (+705..+740, CI excludes 0 the wrong way). (3) P2-compact reported under the frozen nB=4 exclusion (report-only; direction worse). **M1-exit reading (honest):** R2/T1/T7/cyclic PASS, A4/T2 PASS under frozen §13 thresholds, M2_flip anchor PASS, crossings BEAT v2 (123 vs 177 P1-compact), A7 gate passes 5/6 cells, slice-A deviation improves — but the slice-B extent row of the §4 M1-exit table FAILS on the P1-compact frozen cell and the owner's arm-E verdict (BINDING) now adjudicates with this on the table alongside the registered height levers. *(WP-3e extension + orchestrator gate adjudication)*

- **SDEC-45 · 2026-07-05 · Codex review of the W3 diff (V2 gate): 5 P1 + 4 P2 reported; orchestrator adjudication = 5 real fixes dispatched (WP-3f), 4 standing adjudications HOLD.** **Fixes dispatched (all identity/measurement-layer, no layout behavior):** (1) `isStrataCanonicalId` accepted ANY `tf:`-prefixed id — a foreign/pasted element could activate or be caught by the tombstone pass → tightened to the exact role grammar; (2) extent-gate harness silently collapsed duplicate pairing keys first-wins → duplicates now VOID the cell explicitly (empirically zero today, now guaranteed); (3) `applyStrataSweep` compared floating barycenter means with exact equality — the round-7 float-tie ULP hazard class v3.1 §1 was written to kill, resurfacing in the sweep tiebreak → exact integer cross-multiplication on (Σ raw indices, count) rationals (normalization is per-sweep monotone ⇒ order-equivalent); (4) two measurement sorts lacked C4′ total orders (sliceMetrics equal-Y bands, pierce equal-center clusters — input-array-stability-dependent) → content tiebreaks, with an explicit frozen-baseline invariance check (SHA-pinned JSONs must not move); (5) churn rename fold resolved non-injective maps silently later-wins → loud throw. **Standing adjudications that HOLD against codex (each logged with rationale):** (a) G default = 1 / no caller generation source — the SDEC-38a deliberate S7/M3 deferral; the tombstone-version-tie risk codex describes is a COLLAB-reconcile concern and M1 ships no collab (the spec's own OD-7 names the app-side counter as the input mechanism, which S7 owns); (b) icon/inner-group ordinals = in-block first-appearance rank — SDEC-38c: the block is cloned verbatim from the committed icon-library asset, so in-block order IS asset content order; codex's "inserting an asset element changes ids" scenario is a CONTENT change, for which id change is semantically correct; (c) A7 median neighbors = swept-from SIDE (all strictly-earlier/later home columns), not literally column c∓1 — SDEC-41 pin P2: the spec's "neighbors" bullet explicitly includes long edges as direct chords, and under the literal single-column reading a long edge could NEVER enter any median (its endpoints are never in an adjacent column), making the bullet target-inert; codex's "overweights distant columns" is a quality concern for M2 tuning, not a spec violation — logged as an A7 tuning note; (d) the A7 EPS acceptance band (reject improvements < 1e-6 px) is STRICTER than the spec's strict-decrease, which is gate-sound and deterministic. *(codex + orchestrator adjudication; WP-3f fix pass in flight)*

- **SDEC-46 · 2026-07-05 · WP-3f landed (codex-fix pass) — all 5 fixes verified; W3 CODE-COMPLETE, STOPPED at checkpoint V2.** (1) `isStrataCanonicalId` tightened to the exact role grammar (`/^tf:(node|frame|label|icon|edge|dup|group):/` + sanitized-frame prefix); (2) extent-gate duplicate pairing keys now hard-VOID the cell with `voidReason`/`duplicateKeys` surfaced (report shape preserved for clean cells); (3) barycenter tie comparison replaced with exact integer cross-multiplication on (Σ raw indices, count) rationals — **no fixture and no K=4 ordering changed** (the ULP hazard was latent, not manifest — battery tables remain valid without a re-run); (4) C4′ tiebreaks added to the sliceMetrics band-Y sort and the pierce in-column sort, with frozen-baseline invariance BYTE-VERIFIED against the SHA-pinned `Q2_AUDIT_REPORT_P1.json` (pre-change, post-change, and frozen JSON all byte-identical — no number moved, baselines untouched); (5) non-injective rename maps now throw loudly in the churn index. Verification: WP-3f full 17-suite run 222/222 + typecheck + eslint + NUL scans clean; orchestrator independent spot-check of the six fix-touched suites 106/106. **W3 deliverables complete: engine (A2 K=4 + A7 + A6 + tombstones), gate machinery (A4/A5/bootstrap-CI), harnesses (triple + extent gate), frozen thresholds (v3.1 §13), battery report (`docs/strata-view-w3-battery-report.md`), codex review adjudicated (SDEC-45). STOPPED at V2 — owner visual validation + BINDING arm-E verdict + K=4/A7 default decisions + §13/supplementary-pairing freeze review. Commit only on owner OK.** *(WP-3f agent + orchestrator verification)*

- **SDEC-47 · 2026-07-05 · Checkpoint V2 — OWNER VERDICT: ACCEPTED. Arm-E (BINDING) = POSITIVE. W3/M1b committed.** Owner verbatim: *"yeah i guess it looks good, i trust that you implemented this correctly, i guess commit and we loop to the next?"* (after visually validating the K=4+A7 render against v2 via the §5 URLs). **Adjudications this closes:** (1) **Arm-E POSITIVE** — M1b accepted, no redo; the v3.0 §9 owner-adjudicated verdict is the authority that overrides the automatic §2.5 slice-B extent gate, so the SDEC-44 P1-compact extent FAIL is hereby owner-acknowledged-and-accepted (the height/tail regression is a known, registered residual — levers OD-14/OD-15 remain the post-M1 path, not a blocker). (2) **v3.1 §13 A4 threshold freeze (M1_rel ≤ 0.08, M2_flip ≤ 0.10) — ACCEPTED** by trust-delegation; the honest failed-P1→P2-transfer derivation stands as recorded. (3) **Supplementary full-mode extent pairing — stays REPORT-ONLY companion** (primary frozen; P1-full 15.5px-shy result is informational, not a gate pass). (4) **K=4 / A7 default flip — owner DELEGATED to orchestrator** ("i trust that you implemented this correctly"). **Orchestrator decision:** the checkpoint commit lands the exact codex-reviewed, battery-gated artifact with the engine toggles `strataSweeps`/`strataCoordinateRefine` at their tested DEFAULT-OFF values (no behavior change smuggled into the M1b commit); the recommended follow-up is to make **K=4+A7 the Strata-VIEW default** (what the owner actually validated — K=0 is the "unoptimized" V1 reference the owner would NOT expect the button to show), executed as a small separately-tested change (view-threading test per wiring-checklist #15) at the top of the next loop, fully reversible. **Committed:** `c4a60924b` — W3 code + `docs/strata-view-w3-battery-report.md` + this log + v3.1 §13; the pre-existing unrelated dirty files (skills/AGENTS/CLAUDE/package.json/etc.) were absent from the working tree and excluded by construction. Final gate: 222/222 · typecheck clean. **M1 (D10→S0a→M1a→M1b) is COMPLETE.** *(owner verdict + orchestrator commit)*

*(Append new entries below. Every work package lands one entry: what was decided/changed, what the battery said, and — at checkpoints — the owner's verdict verbatim.)*
